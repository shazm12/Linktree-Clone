import React from 'react'

const HeaderPreview = ({ text }) => {
  return (
    <div className='my-5'>
      <h1 className='font-extrabold tracking-tight text-2xl text-center'>{text}</h1>
    </div>
  )
}

export default HeaderPreview
